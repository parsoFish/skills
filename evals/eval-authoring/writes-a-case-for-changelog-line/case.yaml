schema_version: "1.1"
name: writes-a-case-for-changelog-line
description: Write a claude plugin eval case for an existing skill that has none yet, with the correct file shapes and a grader that actually scores the outcome rather than only the fire indicator.
context:
  scaffold_script: fixture.sh
