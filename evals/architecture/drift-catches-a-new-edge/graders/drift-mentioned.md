---
type: regex
target: last_message
pattern: "drift|undeclared"
flags: "i"
---
The source now imports src/util/helper.js from src/cli/main.js, an edge the seeded hand.c4 never declared and the empty drift-baseline.json never accepted. The final reply must say so — it must not report a clean re-check.
