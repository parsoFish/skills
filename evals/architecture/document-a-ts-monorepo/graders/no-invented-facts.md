---
type: llm
---
Score 1 if every claim about the repository's structure in the produced docs is either visibly derived from files in the checkout (paths cited, generated tables present) or explicitly marked as a GAP or interview question. Score 0 if the docs state components, dependencies, risks, or decisions that do not exist in the repository, or present a `.c4` source file or raw JSON as the human-facing deliverable instead of a rendered view or markdown table.
